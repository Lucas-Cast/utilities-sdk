All asked features must be done with clean code principles, as not using more than 1 idented block, opt for early returns or ternary operators

As an example, prefer this syntax: if(condition) return value

Never use try catch blocks, use only .then() .catch() and .finally()
